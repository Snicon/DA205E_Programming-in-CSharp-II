<!-- Sixten Peterson (AQ9300) 2026-05-04 -->
<Window x:Class="DA205E_Assignment6.UI.Windows.CourseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DA205E_Assignment6.UI.Windows"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Title="Add course"
        Height="200"
        Width="300">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Course name -->
        <Label Grid.Row="0" Grid.Column="0" x:Name="lblCourseName" Target="{Binding ElementName=txtCourseName}" Content="Name"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtCourseName" Text="{Binding CourseName}" Margin="40,0,0,0"/>

        <!-- Course code -->
        <Label Grid.Row="1" Grid.Column="0" x:Name="lblCourseCode" Margin="0,10,0,0" Target="{Binding ElementName=txtCourseName}" Content="Code"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtCourseCode" Text="{Binding CourseCode}" Margin="40,10,0,0"/>

        <!-- Add/Edit course button (Content changed in code-behind) -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="btnSubmit" Margin="0,15,5,0" Padding="25,5" Content="Add course" Click="btnSubmit_Click"/>
            <Button x:Name="btnCancel" Margin="5,15,0,0" Padding="25,5" Content="Cancel" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
